-- Criar banco de dados
--CREATE DATABASE radiador;


-- Tabela servicoradiadores
CREATE TABLE servicoradiadores (
    id BIGSERIAL PRIMARY KEY,
    modelo VARCHAR(255) NOT NULL,
    tipo VARCHAR(255) NOT NULL,
    servico_executado VARCHAR(255) NOT NULL,
    cliente VARCHAR(255) NOT NULL,
    data DATE NOT NULL,
    preco NUMERIC(38,2) NOT NULL
);

-- Tabela users
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(255),
    password VARCHAR(255)
);

-- Tabela roles
CREATE TABLE roles (
    id BIGSERIAL PRIMARY KEY,
    name VARCHAR(255)
);

-- Tabela user_roles (tabela de relacionamento)
CREATE TABLE user_roles (
    user_id BIGINT NOT NULL,
    role_id BIGINT NOT NULL,
    PRIMARY KEY (user_id, role_id),
    CONSTRAINT fk_user_roles_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE,
    CONSTRAINT fk_user_roles_role FOREIGN KEY (role_id) REFERENCES roles (id) ON DELETE CASCADE
);

-- Inserir usuário admin
INSERT INTO users (username, password)
VALUES ('admin', 'Oracle#21c');

-- Criar role ROLE_ADMIN caso ainda não exista
INSERT INTO roles (name)
SELECT 'ROLE_ADMIN'
WHERE NOT EXISTS (SELECT 1 FROM roles WHERE name = 'ROLE_ADMIN');

-- Associar admin à ROLE_ADMIN
INSERT INTO user_roles (user_id, role_id)
SELECT u.id, r.id
FROM users u, roles r
WHERE u.username = 'admin' AND r.name = 'ROLE_ADMIN'
ON CONFLICT DO NOTHING;





